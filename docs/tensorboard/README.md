<div align="center">

# 📚 TensorBoard: Руководство по использованию 📚

</div>

---

<div align="center">

## 🌟 Введение 🌟

</div>

TensorBoard — это мощный инструмент для визуализации и мониторинга процесса обучения моделей машинного обучения. Он позволяет отслеживать метрики, визуализировать графики, изображения и даже гистограммы. В этом руководстве мы рассмотрим, как использовать TensorBoard для отслеживания прогресса обучения модели, с акцентом на метрику `g/total`. Этот инструмент особенно полезен для моделей генеративных состязательных сетей (GAN), но также может быть использован для других типов моделей.

---

<div align="center">

## 🚀 Как использовать TensorBoard? 🚀

</div>

### ⚙️ Настройка TensorBoard
1. **Переход на вкладку "Scalars"**:
   - После запуска TensorBoard, перейдите на вкладку «Scalars». Здесь вы найдете графики всех ваших метрик, включая `g/total`.
   - Если графиков слишком много, используйте строку поиска в верхней части интерфейса, чтобы быстро найти нужный.

2. **Сглаживание графика**:
   - Установите сглаживание (smoothing) на `0.950` или `0.987`. Это поможет снизить шум и сделать изменения метрики более четкими и понятными.

3. **Автоматическое обновление данных**:
   - Нажмите на значок шестерёнки (⚙️) в правом верхнем углу.
   - Включите автоматическую перезагрузку данных каждые 30 секунд, чтобы видеть обновления в реальном времени.

4. **Работа с графиками**:
   - Под каждым графиком есть три кнопки:
     - Первая — для увеличения размера графика.
     - Вторая — для отключения оси Y.
     - Третья — для подгонки данных под график.
   - Уберите галочку с опции **«ignore outliers in chart scaling»**, чтобы избежать искажения графика из-за выбросов.

### 📉 Как найти низшую точку?
Низшая точка — это момент, когда график опускается так низко, что больше не повторяется. Во время обучения могут быть несколько таких точек, которые стоит протестировать, чтобы найти оптимальную модель и предотвратить переобучение.

- **Как определить низшую точку**:
  - Визуально найдите самую низкую точку на графике. Используйте комбинацию `alt + колесико мыши`, чтобы увеличить интересующую область.
  - Наведите курсор на эту точку — появится плавающее окно с информацией о ней.
  - Обратите внимание на количество шагов (steps) в этой точке.
  - Перейдите в папку, где сохраняются модели, и найдите эпохи (epochs) с соответствующим шагом.
    > Формат сохранения модели обычно такой: **`ModelName_e10_s100.pth`**, где **`e10`** — это количество эпох, а **`s100`** — шаги.
  - Сохраните модель и протестируйте её.

---

<div align="center">

## 📊 Подробный анализ метрик 📊

</div>

Помимо `loss/g/total`, важно отслеживать другие метрики, такие как `loss/g/mel`, `loss/g/kl` и `loss/d/total`. Если какие-то из этих значений растут и не уменьшаются, это сигнал о переобучении. В начале обучения нормально, что `d/total` растет, а `g/total` падает, но в конечном итоге они должны стабилизироваться. Все метрики должны снижаться или колебаться вокруг похожих значений. Например, если `g/mel` начинает расти вместо падения, это указывает на проблемы в модели. График `g/mel` показывает точность модели в воспроизведении мел-спектрограммы из вашего набора данных, отражая качество аудио.

---

<div align="center">

## 🧠 Показатели потерь: что они значат? 🧠

</div>

Показатели потерь предоставляют ценную обратную связь в процессе обучения GAN, помогая улучшить качество и реалистичность сгенерированных синтетических данных. Мониторинг этих показателей потерь вместе дает полное представление о прогрессе обучения. Успешное обучение обычно демонстрирует баланс между уменьшением потерь генератора _(`loss/g/total`)_ и дискриминатора _(`loss/d/total`)_, а также уменьшением потерь соответствия признаков _(`loss/g/fm`)_, мел-спектрограммы _(`loss/g/mel`)_ и расхождения KL _(`loss/g/kl`)_. Это указывает на то, что генератор производит все более реалистичные синтетические данные, сохраняя при этом схожую внутреннюю структуру и распределение реальных данных.

Оптимальные значения будут варьироваться в зависимости от конкретной задачи и набора данных. Однако, отслеживая тенденции в этих показателях потерь, вы можете получить ценные инсайты в прогресс обучения и выявить потенциальные проблемы, которые могут возникнуть.

> [!NOTE]
> ### (D) Потеря дискриминатора (**`loss/g/total`**):
> Потеря дискриминатора измеряет способность дискриминатора различать реальные и синтетические данные. Обычно она формулируется как кросс-энтропийная потеря, где дискриминатор штрафуется за неправильную классификацию реальных и синтетических образцов.
>
> Уменьшение потери дискриминатора указывает на то, что дискриминатор становится лучше в различении реальных и синтетических данных. Это обычно хороший знак, так как это предполагает, что генератор производит всё более реалистичные синтетические данные. Однако, если потеря дискриминатора становится слишком низкой, это может указывать на то, что генератор просто запоминает реальные данные, а не учится улавливать их внутренние паттерны.

> [!NOTE]
> ### (G) Потеря генератора (**`loss/d/total`**):
> Потеря генератора измеряет способность генератора производить синтетические данные, которые неотличимы от реальных. Обычно она формулируется как соперничающая потеря, где генератор штрафуется за производство данных, которые легко классифицируются как синтетические дискриминатором.
> 
> Уменьшение потери генератора указывает на то, что генератор успешно учится производить реалистичные синтетические данные.

> [!NOTE]
> ### Потеря соответствия признаков (**`loss/g/fm`**):
> Потеря соответствия признаков поощряет генератор производить синтетические данные, которые имеют схожие промежуточные представления признаков с реальными данными. Это достигается путем сравнения промежуточных активаций экстрактора признаков, примененного как к реальным, так и к синтетическим данным.
> 
> Уменьшение потери соответствия признаков указывает на то, что генератор производит синтетические данные с промежуточными представлениями признаков, схожими с реальными данными. Это предполагает, что генератор учится улавливать внутреннюю структуру реальных данных, что способствует реалистичности синтетических данных.

> [!NOTE]
> ### Потеря мел-спектрограммы (**`loss/g/mel`**):
> Потеря мел-спектрограммы сравнивает мел-спектрограммы реальных и синтетических данных. Мел-спектрограммы — это способ представления частотного содержания аудиосигнала, и эта потеря поощряет генератор производить синтетические данные, которые звучат похоже на реальные данные.
> 
> Уменьшение потери мел-спектрограммы указывает на то, что генератор производит синтетические данные с похожим спектральным распределением на реальные данные. Это особенно важно для задач генерации аудио, так как это обеспечивает, что синтетическое аудио звучит похоже на реальное аудио.

> [!NOTE]
> ### Потеря расхождения KL (**`loss/g/kl`**):
> Потеря расхождения KL поощряет генератор производить синтетические данные, которые имеют похожее распределение латентных переменных с реальными данными. Латентные переменные — это представление внутренних факторов, генерирующих данные, и эта потеря обеспечивает, что генератор не просто запоминает реальные данные, а учится улавливать внутренние паттерны в данных.
> 
> Уменьшение потери расхождения KL указывает на то, что генератор производит синтетические данные с похожим распределением латентных переменных с реальными данными. Это предполагает, что генератор не просто запоминает реальные данные, а учится улавливать внутренние паттерны в данных, что приводит к более обобщаемым синтетическим данным.

---

<div align="center">

## 📘 Дополнительная информация 📘

</div>

### Scalars:
- **`loss/g/total`**: Общая потеря генератора. Это измеряет, насколько хорошо генератор может обмануть дискриминатор и производить реалистично звучащее аудио. Меньшее значение обычно лучше.
- **`loss/d/total`**: Общая потеря дискриминатора. Эта потеря отражает, насколько хорошо дискриминатор может различать реальное и сгенерированное аудио. Более высокое значение обычно указывает на хорошо работающий дискриминатор.
- **`learning_rate`**: Текущая скорость обучения оптимизаторов как для генератора, так и для дискриминатора. Это значение обычно уменьшается со временем, как определено планировщиком скорости обучения.
- **`grad_norm_d`**: Градиентная норма для дискриминатора. Мера величины градиентов во время обучения. Она помогает отслеживать, не становятся ли градиенты слишком большими (что может вызвать нестабильность) или слишком маленькими (что может привести к медленному обучению).
- **`grad_norm_g`**: Градиентная норма для генератора. Подобно `grad_norm_d`, это измеряет величину градиентов для генератора.
- **`loss/g/fm`**: Потеря соответствия признаков. Эта потеря поощряет генератор производить аудио, которое соответствует картам признаков, извлеченным дискриминатором, способствуя схожим акустическим характеристикам между реальным и сгенерированным аудио.
- **`loss/g/mel`**: Потеря мел-спектрограммы. Эта потеря измеряет разницу между мел-спектрограммой сгенерированного аудио и целевой мел-спектрограммой. Меньшее значение указывает на лучшее соответствие в спектральных характеристиках.
- **`loss/g/kl`**: Потеря расхождения Kullback-Leibler (KL). Эта потеря измеряет разницу между распределениями латентных переменных, сгенерированных кодировщиком и постериорным кодировщиком. Меньшее значение указывает на лучшее соответствие между этими распределениями, что способствует более стабильной и управляемой модели.
- **`loss/g/{i}`**: Индивидуальные потери генератора для каждого слоя дискриминатора. Эти потери предоставляют более детальный разбор производительности генератора на разных временных шкалах.
- **`loss/d_r/{i}`**: Индивидуальные потери дискриминатора для реального аудио на каждом слое дискриминатора. Эти потери показывают, как хорошо дискриминатор работает на реальном аудио на разных шкалах.
- **`loss/d_g/{i}`**: Индивидуальные потери дискриминатора для сгенерированного аудио на каждом слое дискриминатора. Эти потери указывают на способность дискриминатора различать сгенерированное аудио от реального на разных временных шкалах.

### Images:
- **`slice/mel_org`**: Визуализация мел-спектрограммы целевого аудиосегмента.
- **`slice/mel_gen`**: Визуализация мел-спектрограммы сгенерированного аудиосегмента.
- **`all/mel`**: Визуализация мел-спектрограммы всего целевого аудио.

---

<div align="center">

## 🔍 Интерпретация 🔍

</div>

- **Общая тенденция**: Ищите уменьшение общей потери генератора (`loss/g/total`) и стабильность или увеличение потери дискриминатора (`loss/d/total`).
- **Градиентная норма**: Отслеживайте нормы градиентов, чтобы избежать их чрезмерного увеличения или уменьшения.
- **Компоненты потерь**: Анализируйте отдельные компоненты потерь (например, `loss/g/mel`, `loss/g/kl`), чтобы понять, как работают различные аспекты модели.
- **Мел-спектрограммы**: Сравнивайте изображения мел-спектрограмм (`slice/mel_org` и `slice/mel_gen`), чтобы визуально оценить качество сгенерированного аудио.

---
